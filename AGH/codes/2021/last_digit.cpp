#include <iostream>
#include <functional>
#include <string>

using namespace std;

typedef long long ll;

int main()
{
	cout << 69;
	
    //ll S;
    //cin >> S;

    /*function<void(ll, string)> fun = [&](ll X, string res) {
        if (X > S)
            return;
        if (X == S) {
            cout << res;
            exit (0);
        }
        for (int i = 0; i <= 9; i++) {
            string test = string(i, res.size() + 1);
            cout << test << '\n';
            ll Y = stoi(test);
            //fun(X + Y, "i" + res);
        }
    };

    fun(0, "");

    cout << -1;*/
}
